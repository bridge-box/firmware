# NanoPi R2S — 512MB RAM, Lite модель
# Профиль: friendlyarm_nanopi-r2s
#
# Особенности:
#   - 512MB RAM — только bridge mode (без туннелирования)
#   - Dual Ethernet: eth0 (WAN side) + eth1 (LAN side)
#   - USB 2.0 для Wi-Fi адаптера
